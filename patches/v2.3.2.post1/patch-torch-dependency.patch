diff --git a/pyproject.toml b/pyproject.toml
index a3d4d1a..863bdb5 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -7,7 +7,7 @@ authors = [
     { name = "Albert Gu", email = "agu@cs.cmu.edu" }
 ]
 requires-python = ">= 3.9"
-dynamic = ["version"]
+dynamic = ["dependencies", "version"]
 license = { file = "LICENSE" }  # Include a LICENSE file in your repo
 keywords = ["cuda", "pytorch", "state-space model"]
 classifiers = [
@@ -15,18 +15,6 @@ classifiers = [
     "License :: OSI Approved :: Apache Software License",
     "Operating System :: Unix"
 ]
-dependencies = [
-    "torch",
-    "tilelang==0.1.8",
-    "apache-tvm-ffi<=0.1.9",
-    "quack-kernels>=0.3.4",
-    "triton>=3.5.0",
-    "ninja",
-    "einops",
-    "transformers",
-    "packaging",
-    "setuptools>=61.0.0",
-]
 [project.urls]
 Repository = "https://github.com/state-spaces/mamba"
 
diff --git a/setup.py b/setup.py
index f9c3320..6de2e85 100755
--- a/setup.py
+++ b/setup.py
@@ -269,11 +269,12 @@ def get_package_version():
     with open(Path(this_dir) / PACKAGE_NAME / "__init__.py", "r") as f:
         version_match = re.search(r"^__version__\s*=\s*(.*)$", f.read(), re.MULTILINE)
     public_version = ast.literal_eval(version_match.group(1))
-    local_version = os.environ.get("MAMBA_LOCAL_VERSION")
-    if local_version:
-        return f"{public_version}+{local_version}"
-    else:
-        return str(public_version)
+    # Auto-generate local version from CUDA and PyTorch versions.
+    cuda_version = torch.version.cuda
+    torch_version = f"{TORCH_MAJOR}.{TORCH_MINOR}"
+    if cuda_version:
+        return f"{public_version}+cu.{cuda_version}.torch.{torch_version}"
+    return str(public_version)
 
 
 def get_wheel_url():
@@ -393,7 +394,7 @@ setup(
     },
     python_requires=">=3.9",
     install_requires=[
-        "torch",
+        f"torch=={TORCH_MAJOR}.{TORCH_MINOR}.*",
         "packaging",
         "ninja",
         "einops",
@@ -402,6 +403,7 @@ setup(
         "tilelang==0.1.8",
         "apache-tvm-ffi<=0.1.9",
         "quack-kernels>=0.3.4",
+        "setuptools>=61.0.0",
         # "causal_conv1d>=1.4.0",
     ],
 )
